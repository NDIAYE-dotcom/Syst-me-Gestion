import React from "react";

const SalesEvolution = ({ data, timeRange }) => {
	return (
		<div className="sales-evolution-chart">
			<p>Aucune donnée d'évolution des ventes disponible.</p>
		</div>
	);
};

export default SalesEvolution;
